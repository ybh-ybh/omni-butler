# Windows 桌面悬浮框功能 PRD

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 文档状态 | 待评审 |
| 更新日期 | 2026-09-24 |
| 适用平台 | Windows |
| 关联模块 | 首页、每日待办、时间管理、设置、系统托盘 |

## 1. 背景

Omni Butler 的今日待办和时间记录已经可以在首页完成高频操作，但用户在使用其他应用时，需要先打开或切换到主窗口才能查看任务、开始计时或补记时间。这个过程打断当前工作，也降低了持续记录的意愿。

本功能在 Windows 桌面提供一个轻量、半透明的悬浮卡片，将“今日待办”和时间记录的核心操作放到桌面。它不是始终置顶窗口：普通应用可以覆盖它，用户返回桌面时即可看到并操作。

## 2. 产品目标

### 2.1 目标

- 用户返回桌面后，可以直接查看和处理三个重点象限中的今日待办。
- 用户无需打开主窗口即可新增、编辑、完成今日待办。
- 用户无需进入时间管理页面即可开始、结束或补记时间记录。
- 悬浮框的位置、开关和业务数据状态在应用重启后保持一致。
- 主窗口关闭后，应用可以驻留系统托盘并继续提供悬浮框。

### 2.2 成功标准

- 从桌面查看今日待办不需要打开主窗口。
- 完成待办、开始计时和打开补记弹窗均可通过一次主要点击触发。
- 开关状态和窗口位置在重启后正确恢复。
- 悬浮框不会遮挡普通应用窗口，也不会出现在任务栏和 `Alt + Tab` 列表中。
- 悬浮框与首页、每日待办页和时间管理页使用同一份本地数据，操作结果实时一致。

### 2.3 非目标

第一版不包含以下能力：

- Android 悬浮窗或桌面小组件。
- 跨应用始终置顶。
- 窗口自由缩放或多种尺寸模板。
- 用户自定义透明度、背景图片或卡片内容。
- 开机自动启动 Omni Butler。
- 在悬浮框中展示名言、今日刻度、事件、会员或时间统计图表。
- 新增服务端接口、业务数据库字段或同步协议。

## 3. 用户场景

### 3.1 快速处理待办

用户按 `Win + D` 返回桌面，在悬浮框中查看今天的重点任务，勾选已经完成的任务，或点击标题修改内容。需要查看全部任务时，用户通过“查看全部”打开主窗口的每日待办页。

### 3.2 快速开始与结束计时

用户准备开始一项活动时，在悬浮框中点击“开始”。计时开始后，悬浮框展示进行中状态和累计时长，主操作变为“结束”。活动结束时，用户点击“结束”并补充活动信息。

### 3.3 补记时间

用户发现遗漏记录时，直接点击“补记”，使用现有时间区间编辑器补充记录，不需要先进入时间管理页。

### 3.4 主窗口关闭后继续使用

悬浮框已开启时，用户关闭主窗口，Omni Butler 隐藏到系统托盘，悬浮框继续显示。用户可以从托盘重新打开主窗口或显式退出应用。

## 4. 信息架构与界面结构

### 4.1 设置入口

入口位于：`设置 → 功能管理 → Windows 桌面悬浮框`。

设置行包含：

- 名称：Windows 桌面悬浮框。
- 说明：在桌面显示今日待办和时间记录快捷操作。
- 开关：默认关闭，只保存在当前设备。
- 依赖提示：每日待办和时间管理至少有一个处于开启状态。

当两个依赖模块都已关闭时：

- 未开启过悬浮框：开关不可操作，并显示“请先开启每日待办或时间管理”。
- 悬浮框原本已开启：保留用户的开启偏好，但暂时隐藏窗口，并显示“相关功能均已关闭，悬浮框已暂时隐藏”。任一依赖模块恢复后自动重新显示。

### 4.2 悬浮框布局

```text
┌────────────────────────────────────┐
│ Omni Butler · 今日            ×   │  ← 可拖动区域
├────────────────────────────────────┤
│ 今日待办                     新增  │
│                                    │
│ 紧急且重要                         │
│ ┌ 每次最多显示 5 个根任务 ───────┐ │
│ │ □ 任务 / ▾ 子任务              │ │  ← 象限内独立滚动
│ └────────────────────────────────┘ │
│                                    │
│ 重要不紧急                         │
│ ┌ 每次最多显示 5 个根任务 ───────┐ │
│ │ □ 任务 / ▾ 子任务              │ │  ← 象限内独立滚动
│ └────────────────────────────────┘ │
│                                    │
│ 紧急不重要                         │
│ ┌ 每次最多显示 5 个根任务 ───────┐ │
│ │ □ 任务 / ▾ 子任务              │ │  ← 象限内独立滚动
│ └────────────────────────────────┘ │
│                          查看全部  │
├────────────────────────────────────┤
│ 时间状态                           │
│ 未计时：              开始   补记  │
│ 进行中：01:26:18      结束   补记  │
└────────────────────────────────────┘
```

### 4.3 尺寸与位置

- 目标尺寸为 `420 × 760` 逻辑像素，用户不能缩放。
- 当当前屏幕可用工作区小于目标高度时，高度缩减为“工作区高度减 32 逻辑像素”，卡片内部继续滚动；宽度保持 420 逻辑像素，除非工作区宽度不足，再按相同规则收窄。
- 首次开启时，悬浮框出现在主显示器工作区右下方，与屏幕边缘保持 24 逻辑像素间距，并避开任务栏。
- 用户可以拖动顶部标题区域移动窗口，按钮、列表和滚动区域不能触发窗口拖动。
- 每次移动完成后保存所在显示器与窗口坐标。
- 显示器断开、分辨率变化、缩放比例变化或任务栏位置变化导致窗口越界时，自动将完整窗口约束回当前可用工作区。

### 4.4 视觉要求

- 使用无系统标题栏的圆角卡片窗口。
- 卡片背景采用约 92% 不透明度；文字、图标、按钮和交互反馈保持 100% 不透明。
- 支持浅色、深色和跟随系统模式，语义色沿用当前飞书蓝主题与三个待办象限颜色。
- 支持时可使用 Windows 背景模糊；不支持或性能不足时退化为半透明纯色背景，不影响信息可读性。
- 卡片需要有清晰边界和轻量阴影，壁纸明暗变化时仍能辨认。
- 尊重系统高对比度和减少动态效果设置；高对比度模式下优先保证对比度，可以关闭半透明和模糊效果。

## 5. 功能需求

### 5.1 窗口与桌面行为

| 编号 | 优先级 | 需求 |
|---|---|---|
| FLOAT-001 | P0 | 悬浮框仅在 Windows 提供，默认关闭。 |
| FLOAT-002 | P0 | 开启后立即创建并显示悬浮框，无需重启应用。 |
| FLOAT-003 | P0 | 悬浮框位于桌面层，普通应用窗口可以覆盖；用户显示桌面后能够看到。 |
| FLOAT-004 | P0 | 悬浮框不出现在任务栏和 `Alt + Tab` 列表中。 |
| FLOAT-005 | P0 | 悬浮框固定尺寸、可拖动、不可缩放，并记忆最后位置。 |
| FLOAT-006 | P0 | 点击悬浮框右上角关闭按钮等同于关闭设置中的悬浮框开关。 |
| FLOAT-007 | P0 | 显示器或工作区发生变化时，窗口不能停留在屏幕外。 |
| FLOAT-008 | P1 | 高对比度模式下停用可能影响可读性的透明和模糊效果。 |

### 5.2 今日待办

| 编号 | 优先级 | 需求 |
|---|---|---|
| FLOAT-TODO-001 | P0 | 待办数据范围与首页一致：只展示计划日期为今天且仍有未完成内容的任务树。 |
| FLOAT-TODO-002 | P0 | 只展示紧急且重要、重要不紧急、紧急不重要三个重点象限。 |
| FLOAT-TODO-003 | P0 | 各象限沿用现有截止时间和用户顺序排序规则。 |
| FLOAT-TODO-004 | P0 | 每个象限以根任务计数，列表视口最多同时容纳 5 个根任务行，超出部分可在该象限内独立滚动查看。 |
| FLOAT-TODO-005 | P0 | 有直属未完成子任务的根任务默认展开，用户可以收起或再次展开。子任务占用同一象限滚动区域，不改变悬浮框尺寸。 |
| FLOAT-TODO-006 | P0 | 用户可以勾选完成任务；完成后展示 6 秒撤销入口，行为与首页一致。 |
| FLOAT-TODO-007 | P0 | 点击任务标题在悬浮框内打开现有待办编辑器。 |
| FLOAT-TODO-008 | P0 | 点击“新增”在悬浮框内打开现有待办编辑器，计划日期默认为今天。 |
| FLOAT-TODO-009 | P0 | 点击“查看全部”显示主窗口并进入每日待办页。 |
| FLOAT-TODO-010 | P1 | 每个象限保留独立滚动位置；当天数据刷新或任务数量减少导致越界时自动回到有效位置。 |

根任务展开后，如果子任务使当前视口无法同时展示 5 个根任务，子任务与根任务共同参与该象限的滚动；“最多 5 个根任务”用于确定未展开时的视口基准高度，不表示展开后强制同时显示 5 个根任务。

### 5.3 时间状态

| 编号 | 优先级 | 需求 |
|---|---|---|
| FLOAT-TIME-001 | P0 | 没有进行中记录时展示“开始”和“补记”按钮。 |
| FLOAT-TIME-002 | P0 | 点击“开始”在悬浮框内打开现有开始记录弹窗。 |
| FLOAT-TIME-003 | P0 | 点击“补记”在悬浮框内打开现有补记弹窗。 |
| FLOAT-TIME-004 | P0 | 存在一条进行中记录时，展示记录标题或类别、开始时间和实时累计时长，并将“开始”替换为“结束”。 |
| FLOAT-TIME-005 | P0 | 进行中时长每秒更新，切换日期或系统从休眠恢复后使用绝对时间重新计算，不累计定时器误差。 |
| FLOAT-TIME-006 | P0 | 点击“结束”在悬浮框内打开现有结束记录弹窗。 |
| FLOAT-TIME-007 | P0 | 时间重叠、结束时间无效等校验沿用时间管理模块现有规则。 |
| FLOAT-TIME-008 | P0 | 同步产生多条进行中记录时，展示冲突提示和“处理”入口；点击后显示主窗口并进入时间管理页，不在悬浮框内自动合并或结束记录。 |

### 5.4 功能开关联动

| 编号 | 优先级 | 需求 |
|---|---|---|
| FLOAT-LINK-001 | P0 | “每日待办”关闭时隐藏今日待办分区，不删除任何待办数据。 |
| FLOAT-LINK-002 | P0 | “时间管理”关闭时隐藏时间状态分区，不删除任何时间记录。 |
| FLOAT-LINK-003 | P0 | 两个模块都关闭时隐藏悬浮框，但保留已经开启的悬浮框偏好。 |
| FLOAT-LINK-004 | P0 | 任一模块重新开启后，如果悬浮框偏好仍为开启，立即恢复悬浮框。 |

## 6. 系统托盘与生命周期

### 6.1 主窗口关闭

- 悬浮框开关开启时，用户点击主窗口关闭按钮，主窗口隐藏到系统托盘，应用进程与悬浮框继续运行。
- 悬浮框开关关闭时，主窗口保持当前退出行为，不额外驻留后台。
- 如果主窗口已经隐藏到托盘，用户再从悬浮框关闭该功能，应用保留托盘图标，等待用户重新打开主窗口或显式退出；不会在用户操作后突然结束进程。

### 6.2 托盘菜单

系统托盘菜单至少包含：

1. 打开 Omni Butler：显示并激活主窗口。
2. Windows 桌面悬浮框：带勾选状态，可直接开启或关闭。
3. 退出：关闭主窗口、悬浮框和托盘图标，并安全结束应用进程。

双击托盘图标默认执行“打开 Omni Butler”。

### 6.3 应用启动与退出

- 应用启动时读取本机悬浮框偏好；偏好开启且至少一个依赖模块可用时自动创建悬浮框。
- 第一版不注册 Windows 开机启动，用户需要主动启动 Omni Butler。
- 用户通过托盘“退出”或其他显式退出入口结束应用后，悬浮框必须同步关闭。
- 意外退出不会清除悬浮框开关和位置，下次启动继续恢复。

## 7. 数据与状态

### 7.1 本机偏好

悬浮框新增以下本机状态：

- 是否开启悬浮框。
- 最近一次有效窗口横坐标与纵坐标。
- 最近所在显示器标识；无法匹配时回退到主显示器。

这些状态使用当前设备的本地偏好存储，不进入业务数据库，也不参与 PowerSync。

### 7.2 业务数据

- 今日待办继续通过现有待办 Repository 和 Provider 读取、修改。
- 时间状态继续通过现有时间记录 Repository 和 Provider 读取、修改。
- 悬浮框与主窗口必须共享同一数据库和业务规则，不能复制一套独立缓存作为事实来源。
- 主窗口或同步修改数据后，悬浮框应自动刷新；悬浮框修改数据后，主窗口也应自动刷新。

### 7.3 日期刷新

- 到达本地时间 00:00 后，今日待办立即切换到新日期。
- 跨日进行中的时间记录继续展示，并按现有业务规则保存为一条完整记录。
- 系统时间、时区或夏令时变化后，重新读取当前日期和绝对时间，不沿用旧的日期缓存。

## 8. 页面状态与异常处理

### 8.1 待办状态

- 加载中：各象限显示紧凑加载占位，不阻塞时间操作区。
- 象限为空：显示“暂无任务”，保留象限标题。
- 全部重点象限为空：显示“今天没有重点待办”和“新增”入口。
- 读取失败：显示“待办暂时无法读取”和重试入口，不关闭悬浮框。
- 保存失败：恢复操作前状态并显示错误提示，不允许出现界面已完成但数据未保存的状态。

### 8.2 时间状态

- 加载中：按钮区域显示加载状态，待办区域仍可操作。
- 读取失败：显示“时间状态暂时无法读取”和重试入口。
- 开始、结束或补记保存失败：弹窗保留用户输入并显示现有校验或错误信息。
- 多条进行中记录：不展示错误的单条累计时长，改为冲突状态并引导用户进入时间管理页处理。

### 8.3 窗口异常

- 无法创建悬浮窗口：设置开关恢复为关闭，并展示“悬浮框启动失败，请重试”。
- 无法注册托盘：悬浮框仍可在主窗口运行期间使用，但关闭主窗口前需要明确提示“关闭后悬浮框将一并退出”。
- 背景模糊不可用：自动使用半透明纯色背景，不向用户显示错误。

## 9. 可用性与无障碍

- 支持键盘在标题操作、三个象限、任务行、时间按钮和“查看全部”之间按视觉顺序移动焦点。
- `Enter` 或 `Space` 可以触发当前聚焦操作，`Esc` 关闭当前弹窗但不关闭悬浮框。
- 屏幕阅读器能够播报窗口名称、象限名称、任务完成状态、子任务展开状态、进行中时长和按钮用途。
- 交互目标不小于当前 Windows 客户端通用控件标准。
- 滚轮位于某个象限区域时只滚动该象限；位于其他区域时不误改其他象限的滚动位置。
- 减少动态效果开启时，关闭非必要的出现、展开和状态切换动画。

## 10. 验收标准

### 10.1 开关与恢复

- 初次安装时悬浮框默认关闭。
- 在设置中开启后立即显示，关闭后立即销毁。
- 点击悬浮框关闭按钮后，设置开关同步变为关闭。
- 重启应用后正确恢复开关状态和窗口位置。
- 两个依赖模块都关闭时窗口隐藏，重新开启任一模块后自动恢复。

### 10.2 桌面窗口行为

- `Win + D` 后悬浮框可见。
- 打开普通应用并覆盖悬浮框所在区域后，悬浮框不会压在普通应用上方。
- 悬浮框不出现在任务栏和 `Alt + Tab` 列表。
- 窗口可以拖动但不能缩放。
- 多显示器断开、分辨率变化和任务栏位置变化后，窗口完整位于可见工作区。

### 10.3 待办

- 三个重点象限的数据、排序和首页一致，不展示“不急且不重要”。
- 每个象限未展开时最多同时显示 5 个根任务，超出后只滚动当前象限即可查看。
- 根任务的子任务可以展开和收起，展开不会改变悬浮框尺寸。
- 新增、编辑、完成和撤销操作会立即同步到首页与每日待办页。
- “查看全部”会显示主窗口并进入每日待办页。

### 10.4 时间记录

- 无进行中记录时可以从悬浮框开始和补记。
- 有一条进行中记录时显示准确的实时累计时长，可以结束或补记。
- 多条进行中记录时显示冲突状态，并能进入时间管理页处理。
- 重叠和无效时间不能保存，错误说明与现有时间管理规则一致。
- 跨越 00:00、系统休眠和系统时间变化后，累计时长仍由绝对时间正确计算。

### 10.5 托盘

- 悬浮框开启时关闭主窗口，主窗口隐藏到托盘且悬浮框继续运行。
- 托盘可以打开主窗口、切换悬浮框和退出应用。
- 通过托盘退出后，主窗口、悬浮框和托盘图标均消失，进程正常结束。

### 10.6 视觉与无障碍

- 浅色、深色和跟随系统模式下，文字与控件均清晰可读。
- 高对比度模式下不会因透明效果降低可读性。
- 键盘可访问所有高频操作，焦点顺序与视觉顺序一致。
- Windows 屏幕阅读器能够识别主要分区、状态和操作。

## 11. 技术影响与实现前验证

本节用于限定产品实现边界，不替代详细技术设计。

- 当前客户端已有 `window_manager`，但运行形态仍是 Flutter 单窗口；实现前需要验证独立悬浮窗口、桌面层级和主窗口间的数据共享方案。
- 当前项目尚未接入系统托盘能力；实现前需要验证托盘菜单、主窗口关闭拦截和显式退出流程。
- 需要验证 Windows 10、Windows 11、多显示器、不同缩放比例和任务栏自动隐藏场景。
- 需要优先复用现有待办编辑器、时间记录弹窗、Repository、Provider、主题和反馈组件，避免形成第二套业务规则。
- 技术选型可以新增必要的 Windows 多窗口或托盘依赖，但不得因此修改服务端接口、同步协议或业务数据模型。

## 12. 风险与约束

| 风险 | 影响 | 应对方式 |
|---|---|---|
| Flutter 辅助窗口与主窗口状态共享能力不足 | 数据刷新延迟或需要重复初始化 | 实现前做最小技术验证，确保两个窗口共享同一业务事实来源 |
| Windows 桌面层级在不同系统版本表现不一致 | 悬浮框错误置顶、被桌面图标遮挡或 Win+D 后不可见 | 覆盖 Windows 10/11 实机验证，并提供普通非置顶窗口降级方案 |
| 三个象限各自滚动产生嵌套滚动负担 | 滚轮操作容易滚错区域 | 每个象限使用明确边界和悬停反馈，滚轮只作用于指针所在象限 |
| 半透明背景在复杂壁纸上可读性不足 | 用户无法辨认内容 | 保持高背景不透明度、文字完全不透明，并在高对比度模式禁用透明效果 |
| 关闭到托盘改变用户对“关闭”的预期 | 用户误以为应用已经退出 | 仅在悬浮框开启时启用，并通过首次提示与托盘“退出”提供明确控制 |

## 13. 发布范围

第一版以 Windows 内部测试版本交付，至少覆盖：

- Windows 10 与 Windows 11。
- 单显示器和双显示器。
- 100%、125%、150% 系统缩放。
- 浅色、深色、高对比度和减少动态效果。
- 主窗口显示、最小化、关闭到托盘、托盘恢复和显式退出。

本功能不新增行为遥测或云端采集。是否达到目标以功能验收、稳定性验证和实际使用反馈为准。
