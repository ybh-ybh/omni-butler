# 首次部署复制为 .env，Compose 会自动读取以下配置。
# 数据库密码：使用较长的随机字母数字串，避免连接 URL 中的特殊字符。
POSTGRES_PASSWORD=replace-with-a-long-random-database-password

# 客户端首次连接所需同步密钥：至少 16 个字符。
SYNC_SECRET=replace-with-a-long-random-sync-secret

# API 的宿主机映射端口，容器内部端口固定为 3000。
API_HOST_PORT=3000

# PowerSync 的宿主机映射端口，容器内部端口固定为 8080。
# 同机 Nginx 代理时，两项可分别填写 127.0.0.1:3000、127.0.0.1:8080。
POWERSYNC_HOST_PORT=8080

# 客户端实际连接的 PowerSync 完整地址（包含 http:// 或 https://）。
# 上面的 POWERSYNC_HOST_PORT 只控制 Docker 的宿主机端口映射，
# 不会自动修改下面的 URL；容器内部端口始终为 8080。
# 局域网直连：URL 端口必须与 POWERSYNC_HOST_PORT 指定的宿主机端口一致。
# 例如填写 POWERSYNC_HOST_PORT=9080，此处填写 http://192.168.1.10:9080。
# HTTPS 反向代理：填写带固定 PowerSync 前缀的公网地址；路径必须是
# /omni-butler/powersync/，且末尾的 / 不能省略，
# 例如 https://butler.example.com/omni-butler/powersync/。
# 默认 HTTPS 端口 443 可省略，不填写 Docker 映射端口；若代理对外使用非默认端口则需写明。
# 将示例 IP 或域名替换为自己的服务器地址。
POWERSYNC_PUBLIC_URL=http://192.168.1.10:8080
